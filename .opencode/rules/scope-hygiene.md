# Scope Hygiene

Keep commits and PRs scoped. Stage by explicit path only; never use `git add .`, `git add -A`, `git add -u`, or `git commit -a`.

Do not sweep unrelated working-tree dust into a commit. If another actor changed files, leave those changes intact unless the user explicitly asks you to modify them.
